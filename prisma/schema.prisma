generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

enum BranchStatus {
  ACTIVE
  INACTIVE
}

enum UserRole {
  SUPER_OWNER
  BRANCH_OWNER
  ADMIN
  CASHIER
  TECHNICIAN
  CASH_CUSTODIAN
}

enum UserStatus {
  PENDING
  ACTIVE
  REJECTED
  DISABLED
}

enum IncentiveClassification {
  NONE
  SALES_AGENT
  SENIOR_SALES_AGENT
  TECHNICIAN
  SENIOR_TECHNICIAN
}

enum IncentiveScheduleType {
  EVERY_N_DAYS
  WEEKLY
  MONTHLY
  MANUAL
}

enum IncentiveProgramType {
  ITEM_SALE
  ORDINARY_REPAIR
  BOARD_LEVEL_REPAIR
}

enum IncentiveCycleStatus {
  EARNING
  CUT_OFF
  CLAIMABLE
  CLOSED
}

enum IncentiveClaimStatus {
  UNCLAIMED
  CLAIMED
  APPROVED
  PAID
  EXPIRED
}

enum SettingCategory {
  BUSINESS_RULE
  OPERATION
  DOCUMENT
  SYSTEM_ADMIN
}

enum SettingValueType {
  STRING
  NUMBER
  BOOLEAN
  JSON
  ARRAY
}

enum CustomerStatus {
  ACTIVE
  INACTIVE
}

enum CatalogStatus {
  ACTIVE
  INACTIVE
}

enum InventoryBatchStatus {
  ACTIVE
  DEPLETED
  EXPIRED
  CANCELLED
}

enum InventoryMovementType {
  STOCK_IN
  STOCK_OUT
  ADJUSTMENT_IN
  ADJUSTMENT_OUT
  TRANSFER_IN
  TRANSFER_OUT
  SALE_OUT
  RETURN_IN
  WARRANTY_OUT
  WARRANTY_RETURN
}

enum InventoryMovementSource {
  MANUAL
  PURCHASE
  SALE
  TRANSFER
  RETURN
  WARRANTY
  SERVICE
  SYSTEM
}

enum ItemSerialStatus {
  AVAILABLE
  RESERVED
  IN_TRANSIT
  SOLD
  RETURNED
  WARRANTY
  DAMAGED
  LOST
}

enum QuotationStatus {
  DRAFT
  SENT
  APPROVED
  CONVERTED
  CANCELLED
}

enum SaleStatus {
  COMPLETED
  CANCELLED
  REFUNDED
  PARTIALLY_REFUNDED
}

enum SalePaymentMethod {
  CASH
  GCASH
  BANK_TRANSFER
  CARD
  CREDIT
  OTHER
}

enum SalePaymentStatus {
  PAID
  PARTIALLY_PAID
  UNPAID
  REFUNDED
}

enum InstallmentTerm {
  CASH_PROMO
  STRAIGHT
  MONTH_3
  MONTH_6
  MONTH_9
  MONTH_12
  MONTH_18
  MONTH_24
}

enum CreditAccountStatus {
  ACTIVE
  PAID
  CANCELLED
  DEFAULTED
}

enum CreditCollectionStatus {
  POSTED
  CANCELLED
}

enum ReceivableSourceType {
  SALE
  SERVICE_JOB
}

enum ReceivableProvider {
  CREDIT_CARD
  DEBIT_CARD
  HOMECREDIT
  SALMON
  KYRO
  OTHER_FINANCING
  IN_HOUSE_INSTALLMENT
}

enum ServiceJobStatus {
  PENDING
  IN_PROGRESS
  READY_FOR_RELEASE
  COMPLETED
  CANCELLED
}

enum ServiceRepairType {
  ORDINARY_REPAIR
  BOARD_LEVEL_REPAIR
}

enum ServicePaymentMethod {
  CASH
  GCASH
  BANK_TRANSFER
  CARD
  OTHER
}

enum ServicePaymentStatus {
  POSTED
  CANCELLED
}

enum ServiceReleaseOutcome {
  REPAIRED
  SERVICE_COMPLETED
  UNREPAIRED
  CUSTOMER_PULL_OUT
  NO_FAULT_FOUND
  DECLINED
  OTHER
}

enum IncentiveType {
  SALE_ITEM
  QUOTATION_SERVICE
  SERVICE_JOB
}

enum IncentiveStatus {
  POSTED
  REVERSED
}

enum StockTransferStatus {
  DRAFT
  REQUESTED
  APPROVED
  REJECTED
  POSTED
  CANCELLED
}

enum StockTransferFulfillmentMethod {
  PICKUP
  DELIVERY
}

enum StockTransferFulfillmentStatus {
  PENDING
  PREPARING
  READY_FOR_PICKUP
  IN_TRANSIT
  RECEIVED
}

enum StockTransferPaymentStatus {
  UNPAID
  PARTIALLY_PAID
  PAID
}

enum StockTransferPaymentMethod {
  CASH
  GCASH
  BANK_TRANSFER
  CARD
  OTHER
}

enum StockTransferSettlementStatus {
  POSTED
  CANCELLED
}

model StockTransfer {
  id String @id @default(cuid())

  transferCode String
  status       StockTransferStatus @default(DRAFT)

  transferDate DateTime  @default(now())
  requestedAt  DateTime?
  approvedAt   DateTime?
  rejectedAt   DateTime?
  postedAt     DateTime?
  cancelledAt  DateTime?

  // Null = legacy transfer. New Stock Transfer V2 records use 2.
  workflowVersion Int?

  // Physical fulfillment is independent from approval/payment.
  fulfillmentMethod StockTransferFulfillmentMethod?
  fulfillmentStatus StockTransferFulfillmentStatus?

  preparingAt      DateTime?
  readyForPickupAt DateTime?
  dispatchedAt     DateTime?
  receivedAt       DateTime?

  dispatchedById String?
  receivedById   String?

  fulfillmentReference String?

  // Financial summary.
  // Nullable preserves historical records without inventing values.
  itemSubtotal   Decimal?                    @db.Decimal(12, 2)
  deliveryCharge Decimal?                    @db.Decimal(12, 2)
  grandTotal     Decimal?                    @db.Decimal(12, 2)
  amountPaid     Decimal?                    @db.Decimal(12, 2)
  paymentStatus  StockTransferPaymentStatus?

  notes              String?
  internalNotes      String?
  rejectionReason    String?
  cancellationReason String?

  fromBranchId String
  fromBranch   Branch @relation("StockTransferFromBranch", fields: [fromBranchId], references: [id], onDelete: Restrict)

  toBranchId String
  toBranch   Branch @relation("StockTransferToBranch", fields: [toBranchId], references: [id], onDelete: Restrict)

  requestedById String?
  requestedBy   User?   @relation("StockTransferRequestedBy", fields: [requestedById], references: [id], onDelete: SetNull)

  approvedById String?
  approvedBy   User?   @relation("StockTransferApprovedBy", fields: [approvedById], references: [id], onDelete: SetNull)

  rejectedById String?
  rejectedBy   User?   @relation("StockTransferRejectedBy", fields: [rejectedById], references: [id], onDelete: SetNull)

  postedById String?
  postedBy   User?   @relation("StockTransferPostedBy", fields: [postedById], references: [id], onDelete: SetNull)

  cancelledById String?
  cancelledBy   User?   @relation("StockTransferCancelledBy", fields: [cancelledById], references: [id], onDelete: SetNull)

  createdById String?
  createdBy   User?   @relation("StockTransferCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  updatedById String?
  updatedBy   User?   @relation("StockTransferUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)

  items       StockTransferItem[]
  settlements StockTransferSettlement[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([fromBranchId, transferCode])
  @@index([fromBranchId])
  @@index([toBranchId])
  @@index([status])
  @@index([fulfillmentStatus])
  @@index([paymentStatus])
  @@index([workflowVersion])
  @@index([transferDate])
  @@index([dispatchedAt])
  @@index([receivedAt])
  @@index([requestedById])
  @@index([approvedById])
  @@index([rejectedById])
  @@index([postedById])
  @@index([cancelledById])
  @@index([createdById])
  @@index([updatedById])
}

model StockTransferItem {
  id String @id @default(cuid())

  lineNo      Int
  description String

  quantity Decimal @db.Decimal(12, 2)

  proposedTransferUnitPrice Decimal?  @db.Decimal(12, 2)
  agreedTransferUnitPrice   Decimal?  @db.Decimal(12, 2)
  transferAmount            Decimal?  @db.Decimal(12, 2)
  priceProposedAt           DateTime?
  priceSetAt                DateTime?
  priceLockedAt             DateTime?

  stockTransferId String
  stockTransfer   StockTransfer @relation(fields: [stockTransferId], references: [id], onDelete: Cascade)

  itemId String
  item   Item   @relation("StockTransferSourceItem", fields: [itemId], references: [id], onDelete: Restrict)

  destinationItemId String?
  destinationItem   Item?   @relation("StockTransferDestinationItem", fields: [destinationItemId], references: [id], onDelete: SetNull)

  fromBatchId String?
  fromBatch   InventoryBatch? @relation("StockTransferItemFromBatch", fields: [fromBatchId], references: [id], onDelete: SetNull)

  priceProposedById String?
  priceProposedBy   User?   @relation("StockTransferPriceProposedBy", fields: [priceProposedById], references: [id], onDelete: SetNull)

  priceSetById String?
  priceSetBy   User?   @relation("StockTransferPriceSetBy", fields: [priceSetById], references: [id], onDelete: SetNull)

  serials             StockTransferSerial[]
  dispatchAllocations StockTransferDispatchAllocation[]
  allocations         StockTransferAllocation[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([stockTransferId])
  @@index([itemId])
  @@index([destinationItemId])
  @@index([fromBatchId])
  @@index([priceProposedById])
  @@index([priceSetById])
}

model StockTransferAllocation {
  id String @id @default(cuid())

  quantity                               Decimal @db.Decimal(12, 2)
  acquisitionUnitCostSnapshot            Decimal @db.Decimal(12, 2)
  sourceOperationalUnitCostSnapshot      Decimal @db.Decimal(12, 2)
  destinationOperationalUnitCostSnapshot Decimal @db.Decimal(12, 2)
  transferAmount                         Decimal @db.Decimal(12, 2)

  stockTransferItemId String
  stockTransferItem   StockTransferItem @relation(fields: [stockTransferItemId], references: [id], onDelete: Cascade)

  sourceBatchId String
  sourceBatch   InventoryBatch @relation("StockTransferAllocationSourceBatch", fields: [sourceBatchId], references: [id], onDelete: Restrict)

  destinationBatchId String
  destinationBatch   InventoryBatch @relation("StockTransferAllocationDestinationBatch", fields: [destinationBatchId], references: [id], onDelete: Restrict)

  serials            StockTransferSerial[]
  dispatchAllocation StockTransferDispatchAllocation? @relation("StockTransferDispatchFinalAllocation")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([stockTransferItemId, sourceBatchId])
  @@index([stockTransferItemId])
  @@index([sourceBatchId])
  @@index([destinationBatchId])
}

model StockTransferSerial {
  id String @id @default(cuid())

  serialNumberSnapshot String

  stockTransferItemId String
  stockTransferItem   StockTransferItem @relation(fields: [stockTransferItemId], references: [id], onDelete: Cascade)

  itemSerialId String
  itemSerial   ItemSerial @relation(fields: [itemSerialId], references: [id], onDelete: Restrict)

  dispatchAllocationId String?
  dispatchAllocation   StockTransferDispatchAllocation? @relation(fields: [dispatchAllocationId], references: [id], onDelete: SetNull)

  allocationId String?
  allocation   StockTransferAllocation? @relation(fields: [allocationId], references: [id], onDelete: SetNull)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([stockTransferItemId, itemSerialId])
  @@index([stockTransferItemId])
  @@index([itemSerialId])
  @@index([dispatchAllocationId])
  @@index([allocationId])
  @@index([serialNumberSnapshot])
}

model StockTransferDispatchAllocation {
  id String @id @default(cuid())

  // Exact quantity physically removed from source inventory.
  quantity Decimal @db.Decimal(12, 2)

  // Immutable financial/cost provenance captured at dispatch.
  acquisitionUnitCostSnapshot            Decimal @db.Decimal(12, 2)
  sourceOperationalUnitCostSnapshot      Decimal @db.Decimal(12, 2)
  destinationOperationalUnitCostSnapshot Decimal @db.Decimal(12, 2)
  transferAmount                         Decimal @db.Decimal(12, 2)

  stockTransferItemId String
  stockTransferItem   StockTransferItem @relation(fields: [stockTransferItemId], references: [id], onDelete: Cascade)

  sourceBatchId String
  sourceBatch   InventoryBatch @relation("StockTransferDispatchSourceBatch", fields: [sourceBatchId], references: [id], onDelete: Restrict)

  // Filled only when destination confirms receipt and the existing
  // final StockTransferAllocation has been created.
  finalAllocationId String?                  @unique
  finalAllocation   StockTransferAllocation? @relation("StockTransferDispatchFinalAllocation", fields: [finalAllocationId], references: [id], onDelete: SetNull)

  serials StockTransferSerial[]

  dispatchedAt DateTime  @default(now())
  receivedAt   DateTime?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([stockTransferItemId, sourceBatchId])
  @@index([stockTransferItemId])
  @@index([sourceBatchId])
  @@index([dispatchedAt])
  @@index([receivedAt])
}

model StockTransferSettlement {
  id String @id @default(cuid())

  status StockTransferSettlementStatus @default(POSTED)

  amount        Decimal                    @db.Decimal(12, 2)
  paymentMethod StockTransferPaymentMethod

  referenceNo String?
  notes       String?

  paidAt DateTime @default(now())

  // Snapshot-style actor identity keeps history readable even if
  // account configuration changes later.
  recordedById   String
  recordedByName String?

  cancelledAt        DateTime?
  cancelledById      String?
  cancelledByName    String?
  cancellationReason String?

  stockTransferId String
  stockTransfer   StockTransfer @relation(fields: [stockTransferId], references: [id], onDelete: Restrict)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([stockTransferId])
  @@index([stockTransferId, status])
  @@index([paidAt])
  @@index([recordedById])
  @@index([status])
}

enum PurchaseReceivingStatus {
  DRAFT
  POSTED
  CANCELLED
}

model PurchaseReceiving {
  id String @id @default(cuid())

  receivingCode String
  status        PurchaseReceivingStatus @default(DRAFT)

  receivingDate      DateTime @default(now())
  supplierDeliveryNo String?
  supplierInvoiceNo  String?
  referenceNo        String?

  supplierNameSnapshot    String?
  supplierContactSnapshot String?

  notes              String?
  internalNotes      String?
  cancellationReason String?

  subtotal      Decimal @default(0) @db.Decimal(12, 2)
  totalDiscount Decimal @default(0) @db.Decimal(12, 2)
  grandTotal    Decimal @default(0) @db.Decimal(12, 2)

  postedAt    DateTime?
  cancelledAt DateTime?

  branchId String
  branch   Branch @relation(fields: [branchId], references: [id], onDelete: Restrict)

  supplierId String
  supplier   Supplier @relation(fields: [supplierId], references: [id], onDelete: Restrict)

  purchaseOrderId String?
  purchaseOrder   PurchaseOrder? @relation(fields: [purchaseOrderId], references: [id], onDelete: SetNull)

  createdById String?
  createdBy   User?   @relation("PurchaseReceivingCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  updatedById String?
  updatedBy   User?   @relation("PurchaseReceivingUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)

  postedById String?
  postedBy   User?   @relation("PurchaseReceivingPostedBy", fields: [postedById], references: [id], onDelete: SetNull)

  cancelledById String?
  cancelledBy   User?   @relation("PurchaseReceivingCancelledBy", fields: [cancelledById], references: [id], onDelete: SetNull)

  items PurchaseReceivingItem[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([branchId, receivingCode])
  @@index([branchId])
  @@index([supplierId])
  @@index([purchaseOrderId])
  @@index([status])
  @@index([receivingDate])
  @@index([createdById])
  @@index([updatedById])
  @@index([postedById])
  @@index([cancelledById])
}

model PurchaseReceivingItem {
  id String @id @default(cuid())

  lineNo      Int
  description String

  quantityReceived Decimal @db.Decimal(12, 2)
  unitCost         Decimal @default(0) @db.Decimal(12, 2)
  discountAmount   Decimal @default(0) @db.Decimal(12, 2)
  lineTotal        Decimal @default(0) @db.Decimal(12, 2)

  batchCode  String?
  expiryDate DateTime?

  purchaseReceivingId String
  purchaseReceiving   PurchaseReceiving @relation(fields: [purchaseReceivingId], references: [id], onDelete: Cascade)

  purchaseOrderItemId String?
  purchaseOrderItem   PurchaseOrderItem? @relation(fields: [purchaseOrderItemId], references: [id], onDelete: SetNull)

  itemId String
  item   Item   @relation(fields: [itemId], references: [id], onDelete: Restrict)

  createdAt DateTime                  @default(now())
  updatedAt DateTime                  @updatedAt
  serials   PurchaseReceivingSerial[]

  @@index([purchaseReceivingId])
  @@index([purchaseOrderItemId])
  @@index([itemId])
  @@index([batchCode])
  @@index([expiryDate])
}

enum PurchaseOrderStatus {
  DRAFT
  ORDERED
  PARTIALLY_RECEIVED
  RECEIVED
  CANCELLED
}

model PurchaseReceivingSerial {
  id String @id @default(cuid())

  serialNumber String

  purchaseReceivingItemId String
  purchaseReceivingItem   PurchaseReceivingItem @relation(fields: [purchaseReceivingItemId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([purchaseReceivingItemId, serialNumber])
  @@index([purchaseReceivingItemId])
  @@index([serialNumber])
}

model PurchaseOrder {
  id String @id @default(cuid())

  poCode String
  status PurchaseOrderStatus @default(DRAFT)

  orderDate    DateTime  @default(now())
  expectedDate DateTime?

  supplierNameSnapshot    String?
  supplierContactSnapshot String?

  notes              String?
  internalNotes      String?
  cancellationReason String?

  subtotal      Decimal @default(0) @db.Decimal(12, 2)
  totalDiscount Decimal @default(0) @db.Decimal(12, 2)
  grandTotal    Decimal @default(0) @db.Decimal(12, 2)

  orderedAt   DateTime?
  receivedAt  DateTime?
  cancelledAt DateTime?

  branchId String
  branch   Branch @relation(fields: [branchId], references: [id], onDelete: Restrict)

  supplierId String
  supplier   Supplier @relation(fields: [supplierId], references: [id], onDelete: Restrict)

  createdById String?
  createdBy   User?   @relation("PurchaseOrderCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  updatedById String?
  updatedBy   User?   @relation("PurchaseOrderUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)

  orderedById String?
  orderedBy   User?   @relation("PurchaseOrderOrderedBy", fields: [orderedById], references: [id], onDelete: SetNull)

  cancelledById String?
  cancelledBy   User?   @relation("PurchaseOrderCancelledBy", fields: [cancelledById], references: [id], onDelete: SetNull)

  items PurchaseOrderItem[]

  createdAt          DateTime            @default(now())
  updatedAt          DateTime            @updatedAt
  purchaseReceivings PurchaseReceiving[]

  @@unique([branchId, poCode])
  @@index([branchId])
  @@index([supplierId])
  @@index([status])
  @@index([orderDate])
  @@index([expectedDate])
  @@index([createdById])
  @@index([updatedById])
  @@index([orderedById])
  @@index([cancelledById])
}

model PurchaseOrderItem {
  id String @id @default(cuid())

  lineNo      Int
  description String

  quantity         Decimal @db.Decimal(12, 2)
  receivedQuantity Decimal @default(0) @db.Decimal(12, 2)

  unitCost       Decimal @default(0) @db.Decimal(12, 2)
  discountAmount Decimal @default(0) @db.Decimal(12, 2)
  lineTotal      Decimal @default(0) @db.Decimal(12, 2)

  purchaseOrderId String
  purchaseOrder   PurchaseOrder @relation(fields: [purchaseOrderId], references: [id], onDelete: Cascade)

  itemId String?
  item   Item?   @relation(fields: [itemId], references: [id], onDelete: SetNull)

  createdAt              DateTime                @default(now())
  updatedAt              DateTime                @updatedAt
  purchaseReceivingItems PurchaseReceivingItem[]

  @@index([purchaseOrderId])
  @@index([itemId])
}

enum SupplierStatus {
  ACTIVE
  INACTIVE
}

model Supplier {
  id String @id @default(cuid())

  supplierCode  String
  name          String
  contactPerson String?
  contactNo     String?
  email         String?
  address       String?
  tin           String?
  notes         String?

  status SupplierStatus @default(ACTIVE)

  branchId String?
  branch   Branch? @relation(fields: [branchId], references: [id], onDelete: SetNull)

  createdById String?
  createdBy   User?   @relation("SupplierCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  updatedById String?
  updatedBy   User?   @relation("SupplierUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)

  createdAt          DateTime            @default(now())
  updatedAt          DateTime            @updatedAt
  purchaseOrders     PurchaseOrder[]
  purchaseReceivings PurchaseReceiving[]

  @@unique([branchId, supplierCode])
  @@index([branchId])
  @@index([name])
  @@index([status])
  @@index([createdById])
  @@index([updatedById])
}

enum DeliveryReceiptStatus {
  DRAFT
  ISSUED
  CANCELLED
}

model DeliveryReceipt {
  id String @id @default(cuid())

  drCode String
  status DeliveryReceiptStatus @default(DRAFT)

  drDate DateTime @default(now())

  customerName      String?
  customerAddress   String?
  customerContactNo String?

  preparedByName String?
  receivedByName String?

  notes         String?
  internalNotes String?

  subtotal      Decimal @default(0) @db.Decimal(12, 2)
  totalDiscount Decimal @default(0) @db.Decimal(12, 2)
  grandTotal    Decimal @default(0) @db.Decimal(12, 2)

  issuedAt           DateTime?
  cancelledAt        DateTime?
  cancellationReason String?

  branchId String
  branch   Branch @relation(fields: [branchId], references: [id], onDelete: Restrict)

  saleId String @unique
  sale   Sale   @relation(fields: [saleId], references: [id], onDelete: Restrict)

  createdById String?
  createdBy   User?   @relation("DeliveryReceiptCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  updatedById String?
  updatedBy   User?   @relation("DeliveryReceiptUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)

  issuedById String?
  issuedBy   User?   @relation("DeliveryReceiptIssuedBy", fields: [issuedById], references: [id], onDelete: SetNull)

  cancelledById String?
  cancelledBy   User?   @relation("DeliveryReceiptCancelledBy", fields: [cancelledById], references: [id], onDelete: SetNull)

  items DeliveryReceiptItem[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([branchId, drCode])
  @@index([branchId])
  @@index([saleId])
  @@index([status])
  @@index([drDate])
  @@index([createdById])
  @@index([updatedById])
  @@index([issuedById])
  @@index([cancelledById])
}

model DeliveryReceiptItem {
  id String @id @default(cuid())

  lineNo Int

  itemCodeSnapshot    String?
  itemDescription     String
  quantity            Decimal @db.Decimal(12, 2)
  cashDiscountedPrice Decimal @default(0) @db.Decimal(12, 2)
  amount              Decimal @default(0) @db.Decimal(12, 2)

  deliveryReceiptId String
  deliveryReceipt   DeliveryReceipt @relation(fields: [deliveryReceiptId], references: [id], onDelete: Cascade)

  saleItemId String?
  saleItem   SaleItem? @relation(fields: [saleItemId], references: [id], onDelete: SetNull)

  itemId String?
  item   Item?   @relation(fields: [itemId], references: [id], onDelete: SetNull)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([deliveryReceiptId])
  @@index([saleItemId])
  @@index([itemId])
}

enum ReturnRequestStatus {
  DRAFT
  REQUESTED
  APPROVED
  REJECTED
  COMPLETED
  CANCELLED
}

enum ReturnRefundMethod {
  CASH
  GCASH
  BANK_TRANSFER
  CARD
  STORE_CREDIT
  NONE
}

model ReturnRequest {
  id String @id @default(cuid())

  returnCode String
  status     ReturnRequestStatus @default(DRAFT)

  reason        String
  notes         String?
  internalNotes String?

  refundMethod      ReturnRefundMethod @default(NONE)
  totalRefundAmount Decimal            @default(0) @db.Decimal(12, 2)

  requestedAt DateTime  @default(now())
  approvedAt  DateTime?
  rejectedAt  DateTime?
  completedAt DateTime?
  cancelledAt DateTime?

  rejectionReason    String?
  cancellationReason String?

  branchId String
  branch   Branch @relation(fields: [branchId], references: [id], onDelete: Restrict)

  customerId String?
  customer   Customer? @relation(fields: [customerId], references: [id], onDelete: SetNull)

  saleId String
  sale   Sale   @relation(fields: [saleId], references: [id], onDelete: Restrict)

  createdById String?
  createdBy   User?   @relation("ReturnRequestCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  updatedById String?
  updatedBy   User?   @relation("ReturnRequestUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)

  approvedById String?
  approvedBy   User?   @relation("ReturnRequestApprovedBy", fields: [approvedById], references: [id], onDelete: SetNull)

  rejectedById String?
  rejectedBy   User?   @relation("ReturnRequestRejectedBy", fields: [rejectedById], references: [id], onDelete: SetNull)

  completedById String?
  completedBy   User?   @relation("ReturnRequestCompletedBy", fields: [completedById], references: [id], onDelete: SetNull)

  cancelledById String?
  cancelledBy   User?   @relation("ReturnRequestCancelledBy", fields: [cancelledById], references: [id], onDelete: SetNull)

  items ReturnItem[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([branchId, returnCode])
  @@index([branchId])
  @@index([customerId])
  @@index([saleId])
  @@index([status])
  @@index([refundMethod])
  @@index([requestedAt])
  @@index([createdById])
  @@index([updatedById])
  @@index([approvedById])
  @@index([rejectedById])
  @@index([completedById])
  @@index([cancelledById])
}

model ReturnItem {
  id String @id @default(cuid())

  lineNo      Int
  description String
  reason      String?

  quantity         Decimal @db.Decimal(12, 2)
  unitRefundAmount Decimal @default(0) @db.Decimal(12, 2)
  lineRefundAmount Decimal @default(0) @db.Decimal(12, 2)

  returnRequestId String
  returnRequest   ReturnRequest @relation(fields: [returnRequestId], references: [id], onDelete: Cascade)

  saleItemId String?
  saleItem   SaleItem? @relation(fields: [saleItemId], references: [id], onDelete: SetNull)

  itemId String?
  item   Item?   @relation(fields: [itemId], references: [id], onDelete: SetNull)

  serialId String?
  serial   ItemSerial? @relation(fields: [serialId], references: [id], onDelete: SetNull)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([returnRequestId])
  @@index([saleItemId])
  @@index([itemId])
  @@index([serialId])
}

enum WarrantyClaimStatus {
  IN
  CHECKING
  SENT_TO_SUPPLIER
  APPROVED
  REJECTED
  REPAIRED
  REPLACED
  OUT
}

model WarrantyClaim {
  id String @id @default(cuid())

  claimCode String
  status    WarrantyClaimStatus @default(IN)

  issueDescription    String
  customerComplaint   String?
  diagnosis           String?
  actionTaken         String?
  supplierName        String?
  supplierReferenceNo String?
  remarks             String?

  receivedAt       DateTime  @default(now())
  checkingAt       DateTime?
  sentToSupplierAt DateTime?
  approvedAt       DateTime?
  rejectedAt       DateTime?
  repairedAt       DateTime?
  replacedAt       DateTime?
  releasedAt       DateTime?

  branchId String
  branch   Branch @relation(fields: [branchId], references: [id], onDelete: Restrict)

  customerId String?
  customer   Customer? @relation(fields: [customerId], references: [id], onDelete: SetNull)

  itemId String?
  item   Item?   @relation(fields: [itemId], references: [id], onDelete: SetNull)

  serialId String?
  serial   ItemSerial? @relation(fields: [serialId], references: [id], onDelete: SetNull)

  saleId String?
  sale   Sale?   @relation(fields: [saleId], references: [id], onDelete: SetNull)

  saleItemId String?
  saleItem   SaleItem? @relation(fields: [saleItemId], references: [id], onDelete: SetNull)

  createdById String?
  createdBy   User?   @relation("WarrantyClaimCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  updatedById String?
  updatedBy   User?   @relation("WarrantyClaimUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)

  statusUpdatedById String?
  statusUpdatedBy   User?   @relation("WarrantyClaimStatusUpdatedBy", fields: [statusUpdatedById], references: [id], onDelete: SetNull)

  releasedById String?
  releasedBy   User?   @relation("WarrantyClaimReleasedBy", fields: [releasedById], references: [id], onDelete: SetNull)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([branchId, claimCode])
  @@index([branchId])
  @@index([customerId])
  @@index([itemId])
  @@index([serialId])
  @@index([saleId])
  @@index([saleItemId])
  @@index([status])
  @@index([receivedAt])
  @@index([createdById])
  @@index([updatedById])
  @@index([statusUpdatedById])
  @@index([releasedById])
}

model ServicePayment {
  id String @id @default(cuid())

  paymentCode   String
  paymentMethod ServicePaymentMethod
  status        ServicePaymentStatus @default(POSTED)

  amount      Decimal  @db.Decimal(12, 2)
  referenceNo String?
  remarks     String?
  paidAt      DateTime @default(now())

  idempotencyKey         String?
  idempotencyFingerprint String?

  serviceJobId String
  serviceJob   ServiceJob @relation(fields: [serviceJobId], references: [id], onDelete: Restrict)

  branchId String
  branch   Branch @relation(fields: [branchId], references: [id], onDelete: Restrict)

  customerId String?
  customer   Customer? @relation(fields: [customerId], references: [id], onDelete: SetNull)

  collectedById String?
  collectedBy   User?   @relation("ServicePaymentCollectedBy", fields: [collectedById], references: [id], onDelete: SetNull)

  createdById String?
  createdBy   User?   @relation("ServicePaymentCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  cancelledAt        DateTime?
  cancellationReason String?

  cancelledById String?
  cancelledBy   User?   @relation("ServicePaymentCancelledBy", fields: [cancelledById], references: [id], onDelete: SetNull)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([branchId, paymentCode])
  @@unique([branchId, idempotencyKey])
  @@index([serviceJobId])
  @@index([branchId])
  @@index([customerId])
  @@index([paymentMethod])
  @@index([status])
  @@index([paidAt])
  @@index([collectedById])
  @@index([createdById])
  @@index([cancelledById])
}

model ServiceJob {
  id String @id @default(cuid())

  jobCode    String
  status     ServiceJobStatus   @default(PENDING)
  repairType ServiceRepairType?

  jobTitle                String
  deviceDescription       String?
  problemDescription      String?
  diagnosis               String?
  serviceNotes            String?
  customerNameSnapshot    String?
  customerContactSnapshot String?
  serialNumber            String?
  accessoriesReceived     String?
  receivingRemarks        String?
  isQuickService          Boolean @default(false)

  estimatedServiceCharge Decimal  @default(0) @db.Decimal(12, 2)
  baseServiceCharge      Decimal? @db.Decimal(12, 2)
  markupPercent          Decimal? @db.Decimal(7, 4)
  finalServiceCharge     Decimal  @default(0) @db.Decimal(12, 2)
  serviceMarkupAmount    Decimal? @db.Decimal(12, 2)

  repairCostPercentSnapshot         Decimal?  @db.Decimal(7, 4)
  companySharePercentSnapshot       Decimal?  @db.Decimal(7, 4)
  repairCostPoolAmountSnapshot      Decimal?  @db.Decimal(12, 2)
  companyShareAmountSnapshot        Decimal?  @db.Decimal(12, 2)
  repairFeeSnapshot                 Decimal?  @db.Decimal(12, 2)
  repairIncentiveRateSnapshot       Decimal?  @db.Decimal(7, 4)
  repairIncentiveAmountSnapshot     Decimal?  @db.Decimal(12, 2)
  unallocatedRepairCostPoolSnapshot Decimal?  @db.Decimal(12, 2)
  financialSnapshotAt               DateTime?

  receivedAt  DateTime  @default(now())
  startedAt   DateTime?
  readyAt     DateTime?
  completedAt DateTime?
  cancelledAt DateTime?
  releasedAt  DateTime?

  cancellationReason String?
  releaseOutcome     ServiceReleaseOutcome?
  releaseNotes       String?

  branchId String
  branch   Branch @relation(fields: [branchId], references: [id], onDelete: Restrict)

  customerId String?
  customer   Customer? @relation(fields: [customerId], references: [id], onDelete: SetNull)

  assignedTechnicianId String?
  assignedTechnician   User?   @relation("ServiceJobAssignedTechnician", fields: [assignedTechnicianId], references: [id], onDelete: SetNull)

  serviceDoneById                     String?
  serviceDoneBy                       User?                    @relation("ServiceJobServiceDoneBy", fields: [serviceDoneById], references: [id], onDelete: Restrict)
  serviceDoneByClassificationSnapshot IncentiveClassification?

  programRuleVersionId String?
  programRuleVersion   IncentiveProgramRuleVersion? @relation("ServiceJobProgramRuleVersion", fields: [programRuleVersionId], references: [id], onDelete: Restrict)

  accountConfigVersionId String?
  accountConfigVersion   IncentiveAccountConfigVersion? @relation("ServiceJobAccountConfigVersion", fields: [accountConfigVersionId], references: [id], onDelete: Restrict)

  createdById String?
  createdBy   User?   @relation("ServiceJobCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  updatedById String?
  updatedBy   User?   @relation("ServiceJobUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)

  cancelledById String?
  cancelledBy   User?   @relation("ServiceJobCancelledBy", fields: [cancelledById], references: [id], onDelete: SetNull)

  releasedById String?
  releasedBy   User?   @relation("ServiceJobReleasedBy", fields: [releasedById], references: [id], onDelete: SetNull)

  createdAt     DateTime         @default(now())
  updatedAt     DateTime         @updatedAt
  payments      ServicePayment[]
  creditAccount CreditAccount?
  incentives    Incentive[]

  @@unique([branchId, jobCode])
  @@index([branchId])
  @@index([customerId])
  @@index([assignedTechnicianId])
  @@index([serviceDoneById])
  @@index([programRuleVersionId])
  @@index([accountConfigVersionId])
  @@index([status])
  @@index([repairType])
  @@index([receivedAt])
  @@index([createdById])
  @@index([updatedById])
  @@index([cancelledById])
  @@index([releasedById])
  @@index([releasedAt])
  @@index([releaseOutcome])
  @@index([isQuickService])
}

model Branch {
  id        String       @id @default(cuid())
  code      String       @unique
  name      String
  address   String?
  contactNo String?
  status    BranchStatus @default(ACTIVE)

  users            User[]
  auditLogs        AuditLog[]
  businessSettings BusinessSetting[]
  itemCategories   ItemCategory[]
  items            Item[]
  customers        Customer[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  inventoryBatches                 InventoryBatch[]
  inventoryMovements               InventoryMovement[]
  itemSerials                      ItemSerial[]
  quotations                       Quotation[]
  sales                            Sale[]
  creditAccounts                   CreditAccount[]
  creditCollections                CreditCollection[]
  cashBoxes                        CashBox[]
  cashTransactions                 CashTransaction[]
  cashHandovers                    CashHandover[]
  cashCustodianAssignments         CashCustodianAssignment[]
  serviceJobs                      ServiceJob[]
  servicePayments                  ServicePayment[]
  warrantyClaims                   WarrantyClaim[]
  returnRequests                   ReturnRequest[]
  deliveryReceipts                 DeliveryReceipt[]
  suppliers                        Supplier[]
  purchaseOrders                   PurchaseOrder[]
  purchaseReceivings               PurchaseReceiving[]
  stockTransfersFrom               StockTransfer[]                   @relation("StockTransferFromBranch")
  stockTransfersTo                 StockTransfer[]                   @relation("StockTransferToBranch")
  incentives                       Incentive[]
  incentiveClaims                  IncentiveClaim[]
  incentiveProgramRuleVersions     IncentiveProgramRuleVersion[]
  incentiveProgramScheduleVersions IncentiveProgramScheduleVersion[]
  incentiveV2Cycles                IncentiveCycle[]                  @relation("IncentiveCycleV2Branch")

  @@index([status])
  @@index([name])
}

model User {
  createdItemCategories ItemCategory[] @relation("ItemCategoryCreatedBy")
  updatedItemCategories ItemCategory[] @relation("ItemCategoryUpdatedBy")
  createdUnits          Unit[]         @relation("UnitCreatedBy")
  updatedUnits          Unit[]         @relation("UnitUpdatedBy")
  createdItems          Item[]         @relation("ItemCreatedBy")
  updatedItems          Item[]         @relation("ItemUpdatedBy")
  id                    String         @id @default(cuid())
  employeeCode          String?        @unique
  username              String         @unique
  email                 String?        @unique
  passwordHash          String

  firstName  String
  middleName String?
  lastName   String
  fullName   String

  role                    UserRole
  status                  UserStatus              @default(PENDING)
  incentiveClassification IncentiveClassification @default(NONE)

  branchId String?
  branch   Branch? @relation(fields: [branchId], references: [id], onDelete: SetNull)

  approvedById  String?
  approvedBy    User?   @relation("UserApprovals", fields: [approvedById], references: [id], onDelete: SetNull)
  approvedUsers User[]  @relation("UserApprovals")

  approvedAt  DateTime?
  rejectedAt  DateTime?
  disabledAt  DateTime?
  lastLoginAt DateTime?

  auditLogs        AuditLog[]        @relation("AuditActor")
  updatedSettings  BusinessSetting[] @relation("SettingUpdatedBy")
  createdCustomers Customer[]        @relation("CustomerCreatedBy")
  updatedCustomers Customer[]        @relation("CustomerUpdatedBy")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  createdInventoryBatches    InventoryBatch[]    @relation("InventoryBatchCreatedBy")
  updatedInventoryBatches    InventoryBatch[]    @relation("InventoryBatchUpdatedBy")
  createdInventoryMovements  InventoryMovement[] @relation("InventoryMovementCreatedBy")
  updatedInventoryMovements  InventoryMovement[] @relation("InventoryMovementUpdatedBy")
  createdItemSerials         ItemSerial[]        @relation("ItemSerialCreatedBy")
  updatedItemSerials         ItemSerial[]        @relation("ItemSerialUpdatedBy")
  preparedQuotations         Quotation[]         @relation("QuotationPreparedBy")
  serviceDoneQuotations      Quotation[]         @relation("QuotationServiceDoneBy")
  createdQuotations          Quotation[]         @relation("QuotationCreatedBy")
  updatedQuotations          Quotation[]         @relation("QuotationUpdatedBy")
  cashierSales               Sale[]              @relation("SaleCashier")
  createdSales               Sale[]              @relation("SaleCreatedBy")
  updatedSales               Sale[]              @relation("SaleUpdatedBy")
  cancelledSales             Sale[]              @relation("SaleCancelledBy")
  createdSalePayments        SalePayment[]       @relation("SalePaymentCreatedBy")
  createdCreditAccounts      CreditAccount[]     @relation("CreditAccountCreatedBy")
  updatedCreditAccounts      CreditAccount[]     @relation("CreditAccountUpdatedBy")
  cancelledCreditAccounts    CreditAccount[]     @relation("CreditAccountCancelledBy")
  collectedCreditCollections CreditCollection[]  @relation("CreditCollectionCollectedBy")
  createdCreditCollections   CreditCollection[]  @relation("CreditCollectionCreatedBy")
  cancelledCreditCollections CreditCollection[]  @relation("CreditCollectionCancelledBy")
  createdCashBoxes           CashBox[]           @relation("CashBoxCreatedBy")
  updatedCashBoxes           CashBox[]           @relation("CashBoxUpdatedBy")
  createdCashTransactions    CashTransaction[]   @relation("CashTransactionCreatedBy")
  cancelledCashTransactions  CashTransaction[]   @relation("CashTransactionCancelledBy")
  cashHandoversFrom          CashHandover[]      @relation("CashHandoverFromUser")
  cashHandoversTo            CashHandover[]      @relation("CashHandoverToUser")
  createdCashHandovers       CashHandover[]      @relation("CashHandoverCreatedBy")
  receivedCashHandovers      CashHandover[]      @relation("CashHandoverReceivedBy")
  cancelledCashHandovers     CashHandover[]      @relation("CashHandoverCancelledBy")

  cashCustodianAssignments              CashCustodianAssignment[]         @relation("CashCustodianAssignedUser")
  assignedCashCustodians                CashCustodianAssignment[]         @relation("CashCustodianAssignedBy")
  endedCashCustodianAssignments         CashCustodianAssignment[]         @relation("CashCustodianEndedBy")
  assignedServiceJobs                   ServiceJob[]                      @relation("ServiceJobAssignedTechnician")
  performedServiceJobs                  ServiceJob[]                      @relation("ServiceJobServiceDoneBy")
  createdServiceJobs                    ServiceJob[]                      @relation("ServiceJobCreatedBy")
  updatedServiceJobs                    ServiceJob[]                      @relation("ServiceJobUpdatedBy")
  cancelledServiceJobs                  ServiceJob[]                      @relation("ServiceJobCancelledBy")
  releasedServiceJobs                   ServiceJob[]                      @relation("ServiceJobReleasedBy")
  collectedServicePayments              ServicePayment[]                  @relation("ServicePaymentCollectedBy")
  createdServicePayments                ServicePayment[]                  @relation("ServicePaymentCreatedBy")
  cancelledServicePayments              ServicePayment[]                  @relation("ServicePaymentCancelledBy")
  createdWarrantyClaims                 WarrantyClaim[]                   @relation("WarrantyClaimCreatedBy")
  updatedWarrantyClaims                 WarrantyClaim[]                   @relation("WarrantyClaimUpdatedBy")
  statusUpdatedWarrantyClaims           WarrantyClaim[]                   @relation("WarrantyClaimStatusUpdatedBy")
  releasedWarrantyClaims                WarrantyClaim[]                   @relation("WarrantyClaimReleasedBy")
  createdReturnRequests                 ReturnRequest[]                   @relation("ReturnRequestCreatedBy")
  updatedReturnRequests                 ReturnRequest[]                   @relation("ReturnRequestUpdatedBy")
  approvedReturnRequests                ReturnRequest[]                   @relation("ReturnRequestApprovedBy")
  rejectedReturnRequests                ReturnRequest[]                   @relation("ReturnRequestRejectedBy")
  completedReturnRequests               ReturnRequest[]                   @relation("ReturnRequestCompletedBy")
  cancelledReturnRequests               ReturnRequest[]                   @relation("ReturnRequestCancelledBy")
  createdDeliveryReceipts               DeliveryReceipt[]                 @relation("DeliveryReceiptCreatedBy")
  updatedDeliveryReceipts               DeliveryReceipt[]                 @relation("DeliveryReceiptUpdatedBy")
  issuedDeliveryReceipts                DeliveryReceipt[]                 @relation("DeliveryReceiptIssuedBy")
  cancelledDeliveryReceipts             DeliveryReceipt[]                 @relation("DeliveryReceiptCancelledBy")
  createdSuppliers                      Supplier[]                        @relation("SupplierCreatedBy")
  updatedSuppliers                      Supplier[]                        @relation("SupplierUpdatedBy")
  createdPurchaseOrders                 PurchaseOrder[]                   @relation("PurchaseOrderCreatedBy")
  updatedPurchaseOrders                 PurchaseOrder[]                   @relation("PurchaseOrderUpdatedBy")
  orderedPurchaseOrders                 PurchaseOrder[]                   @relation("PurchaseOrderOrderedBy")
  cancelledPurchaseOrders               PurchaseOrder[]                   @relation("PurchaseOrderCancelledBy")
  createdPurchaseReceivings             PurchaseReceiving[]               @relation("PurchaseReceivingCreatedBy")
  updatedPurchaseReceivings             PurchaseReceiving[]               @relation("PurchaseReceivingUpdatedBy")
  postedPurchaseReceivings              PurchaseReceiving[]               @relation("PurchaseReceivingPostedBy")
  cancelledPurchaseReceivings           PurchaseReceiving[]               @relation("PurchaseReceivingCancelledBy")
  requestedStockTransfers               StockTransfer[]                   @relation("StockTransferRequestedBy")
  approvedStockTransfers                StockTransfer[]                   @relation("StockTransferApprovedBy")
  rejectedStockTransfers                StockTransfer[]                   @relation("StockTransferRejectedBy")
  postedStockTransfers                  StockTransfer[]                   @relation("StockTransferPostedBy")
  cancelledStockTransfers               StockTransfer[]                   @relation("StockTransferCancelledBy")
  createdStockTransfers                 StockTransfer[]                   @relation("StockTransferCreatedBy")
  updatedStockTransfers                 StockTransfer[]                   @relation("StockTransferUpdatedBy")
  proposedStockTransferPrices           StockTransferItem[]               @relation("StockTransferPriceProposedBy")
  setStockTransferPrices                StockTransferItem[]               @relation("StockTransferPriceSetBy")
  incentiveCredits                      Incentive[]                       @relation("IncentiveStaff")
  postedIncentives                      Incentive[]                       @relation("IncentivePostedBy")
  reversedIncentives                    Incentive[]                       @relation("IncentiveReversedBy")
  createdIncentiveRateVersions          IncentiveRateVersion[]            @relation("IncentiveRateVersionCreatedBy")
  createdIncentiveScheduleVersions      IncentiveScheduleVersion[]        @relation("IncentiveScheduleVersionCreatedBy")
  incentiveAccountConfigVersions        IncentiveAccountConfigVersion[]   @relation("IncentiveAccountConfigAccount")
  createdIncentiveAccountConfigVersions IncentiveAccountConfigVersion[]   @relation("IncentiveAccountConfigCreatedBy")
  createdIncentiveProgramRuleVersions   IncentiveProgramRuleVersion[]     @relation("IncentiveProgramRuleCreatedBy")
  createdIncentiveProgramSchedules      IncentiveProgramScheduleVersion[] @relation("IncentiveProgramScheduleCreatedBy")
  closedIncentiveCycles                 IncentiveCycle[]                  @relation("IncentiveCycleClosedBy")
  incentiveClaims                       IncentiveClaim[]                  @relation("IncentiveClaimStaff")
  submittedIncentiveClaims              IncentiveClaim[]                  @relation("IncentiveClaimClaimedBy")
  approvedIncentiveClaims               IncentiveClaim[]                  @relation("IncentiveClaimApprovedBy")
  paidIncentiveClaims                   IncentiveClaim[]                  @relation("IncentiveClaimPaidBy")
  createdIncentiveItemCycleRevisions    IncentiveItemCycleRevision[]      @relation("IncentiveItemCycleRevisionCreatedBy")
  reversedIncentiveItemCycleRevisions   IncentiveItemCycleRevision[]      @relation("IncentiveItemCycleRevisionReversedBy")
  incentiveItemRecipientSnapshots       IncentiveItemRecipientSnapshot[]  @relation("IncentiveItemRecipientSnapshotStaff")

  @@index([role])
  @@index([status])
  @@index([branchId])
  @@index([fullName])
  @@index([role, status])
  @@index([branchId, role])
  @@index([branchId, status])
  @@index([incentiveClassification])
}

model AuditLog {
  id String @id @default(cuid())

  actorId String?
  actor   User?   @relation("AuditActor", fields: [actorId], references: [id], onDelete: SetNull)

  branchId String?
  branch   Branch? @relation(fields: [branchId], references: [id], onDelete: SetNull)

  action      String
  entityType  String
  entityId    String?
  description String?
  metadata    Json?

  ipAddress String?
  userAgent String?

  createdAt DateTime @default(now())

  @@index([actorId])
  @@index([branchId])
  @@index([action])
  @@index([entityType])
  @@index([entityId])
  @@index([createdAt])
  @@index([branchId, createdAt])
  @@index([actorId, createdAt])
  @@index([entityType, entityId])
}

model BusinessSetting {
  id String @id @default(cuid())

  scopeKey  String           @unique
  key       String
  category  SettingCategory
  valueType SettingValueType
  value     Json

  label       String
  description String?

  isEditable Boolean @default(true)
  isActive   Boolean @default(true)

  branchId String?
  branch   Branch? @relation(fields: [branchId], references: [id], onDelete: Cascade)

  updatedById String?
  updatedBy   User?   @relation("SettingUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([key])
  @@index([category])
  @@index([valueType])
  @@index([branchId])
  @@index([isActive])
  @@index([updatedById])
  @@index([category, isActive])
}

model Customer {
  id           String         @id @default(cuid())
  customerCode String
  fullName     String
  mobileNumber String?
  email        String?
  address      String?
  companyName  String?
  notes        String?
  status       CustomerStatus @default(ACTIVE)
  priceTier    Int            @default(1)

  branchId String
  branch   Branch @relation(fields: [branchId], references: [id], onDelete: Restrict)

  createdById String?
  createdBy   User?   @relation("CustomerCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  updatedById String?
  updatedBy   User?   @relation("CustomerUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)

  createdAt         DateTime           @default(now())
  updatedAt         DateTime           @updatedAt
  quotations        Quotation[]
  sales             Sale[]
  creditAccounts    CreditAccount[]
  creditCollections CreditCollection[]
  serviceJobs       ServiceJob[]
  servicePayments   ServicePayment[]
  warrantyClaims    WarrantyClaim[]
  returnRequests    ReturnRequest[]

  @@unique([branchId, customerCode])
  @@index([branchId])
  @@index([status])
  @@index([priceTier])
  @@index([fullName])
  @@index([mobileNumber])
  @@index([email])
  @@index([createdById])
  @@index([updatedById])
  @@index([branchId, status])
}

model ItemCategory {
  id           String        @id @default(cuid())
  categoryCode String
  name         String
  description  String?
  status       CatalogStatus @default(ACTIVE)

  branchId String
  branch   Branch @relation(fields: [branchId], references: [id], onDelete: Restrict)

  createdById String?
  createdBy   User?   @relation("ItemCategoryCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  updatedById String?
  updatedBy   User?   @relation("ItemCategoryUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)

  items Item[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([branchId, categoryCode])
  @@unique([branchId, name])
  @@index([branchId])
  @@index([status])
  @@index([createdById])
  @@index([updatedById])
}

model Unit {
  id          String        @id @default(cuid())
  unitCode    String        @unique
  name        String        @unique
  description String?
  status      CatalogStatus @default(ACTIVE)

  createdById String?
  createdBy   User?   @relation("UnitCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  updatedById String?
  updatedBy   User?   @relation("UnitUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)

  items Item[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([status])
  @@index([createdById])
  @@index([updatedById])
}

model Item {
  id          String        @id @default(cuid())
  itemCode    String
  itemName    String
  description String?
  barcode     String?
  brand       String?
  modelName   String?
  status      CatalogStatus @default(ACTIVE)

  isSerialized Boolean @default(false)
  hasWarranty  Boolean @default(false)

  costPrice Decimal @default(0) @db.Decimal(12, 2)
  price1    Decimal @default(0) @db.Decimal(12, 2)
  price2    Decimal @default(0) @db.Decimal(12, 2)
  price3    Decimal @default(0) @db.Decimal(12, 2)
  price4    Decimal @default(0) @db.Decimal(12, 2)
  price5    Decimal @default(0) @db.Decimal(12, 2)

  minimumStock Decimal @default(0) @db.Decimal(12, 2)
  reorderLevel Decimal @default(0) @db.Decimal(12, 2)

  branchId String
  branch   Branch @relation(fields: [branchId], references: [id], onDelete: Restrict)

  categoryId String
  category   ItemCategory @relation(fields: [categoryId], references: [id], onDelete: Restrict)

  unitId String
  unit   Unit   @relation(fields: [unitId], references: [id], onDelete: Restrict)

  createdById String?
  createdBy   User?   @relation("ItemCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  updatedById String?
  updatedBy   User?   @relation("ItemUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)

  createdAt                DateTime                @default(now())
  updatedAt                DateTime                @updatedAt
  inventoryBatches         InventoryBatch[]
  inventoryMovements       InventoryMovement[]
  itemSerials              ItemSerial[]
  quotationItems           QuotationItem[]
  saleItems                SaleItem[]
  warrantyClaims           WarrantyClaim[]
  returnItems              ReturnItem[]
  deliveryReceiptItems     DeliveryReceiptItem[]
  purchaseOrderItems       PurchaseOrderItem[]
  purchaseReceivingItems   PurchaseReceivingItem[]
  stockTransferItems       StockTransferItem[]     @relation("StockTransferSourceItem")
  destinationTransferItems StockTransferItem[]     @relation("StockTransferDestinationItem")

  @@unique([branchId, itemCode])
  @@index([branchId])
  @@index([categoryId])
  @@index([unitId])
  @@index([status])
  @@index([barcode])
  @@index([brand])
  @@index([itemName])
  @@index([isSerialized])
  @@index([hasWarranty])
  @@index([createdById])
  @@index([updatedById])
  @@index([branchId, status])
}

model InventoryBatch {
  id String @id @default(cuid())

  batchCode         String
  quantityIn        Decimal @db.Decimal(12, 2)
  quantityAvailable Decimal @default(0) @db.Decimal(12, 2)

  unitCost            Decimal  @default(0) @db.Decimal(12, 2)
  operationalUnitCost Decimal? @db.Decimal(12, 2)
  sellingPrice1       Decimal  @default(0) @db.Decimal(12, 2)
  sellingPrice2       Decimal  @default(0) @db.Decimal(12, 2)
  sellingPrice3       Decimal  @default(0) @db.Decimal(12, 2)
  sellingPrice4       Decimal  @default(0) @db.Decimal(12, 2)
  sellingPrice5       Decimal  @default(0) @db.Decimal(12, 2)

  supplierName String?
  referenceNo  String?
  remarks      String?

  receivedAt DateTime  @default(now())
  expiryDate DateTime?

  status InventoryBatchStatus @default(ACTIVE)

  branchId String
  branch   Branch @relation(fields: [branchId], references: [id], onDelete: Restrict)

  itemId String
  item   Item   @relation(fields: [itemId], references: [id], onDelete: Restrict)

  originBatchId  String?
  originBatch    InventoryBatch?  @relation("InventoryBatchOrigin", fields: [originBatchId], references: [id], onDelete: SetNull)
  derivedBatches InventoryBatch[] @relation("InventoryBatchOrigin")

  createdById String?
  createdBy   User?   @relation("InventoryBatchCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  updatedById String?
  updatedBy   User?   @relation("InventoryBatchUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)

  movements InventoryMovement[]
  serials   ItemSerial[]

  createdAt                       DateTime                          @default(now())
  updatedAt                       DateTime                          @updatedAt
  saleItems                       SaleItem[]
  stockTransferItemsFrom          StockTransferItem[]               @relation("StockTransferItemFromBatch")
  transferDispatchAllocationsFrom StockTransferDispatchAllocation[] @relation("StockTransferDispatchSourceBatch")
  transferAllocationsFrom         StockTransferAllocation[]         @relation("StockTransferAllocationSourceBatch")
  transferAllocationsTo           StockTransferAllocation[]         @relation("StockTransferAllocationDestinationBatch")

  @@unique([branchId, batchCode])
  @@index([branchId])
  @@index([itemId])
  @@index([branchId, itemId])
  @@index([status])
  @@index([expiryDate])
  @@index([originBatchId])
}

model InventoryMovement {
  id String @id @default(cuid())

  movementCode String

  type   InventoryMovementType
  source InventoryMovementSource @default(MANUAL)

  quantity Decimal @db.Decimal(12, 2)

  previousQuantity Decimal? @db.Decimal(12, 2)
  newQuantity      Decimal? @db.Decimal(12, 2)

  unitCost    Decimal? @db.Decimal(12, 2)
  referenceNo String?
  remarks     String?

  movementDate DateTime @default(now())

  branchId String
  branch   Branch @relation(fields: [branchId], references: [id], onDelete: Restrict)

  itemId String
  item   Item   @relation(fields: [itemId], references: [id], onDelete: Restrict)

  batchId String?
  batch   InventoryBatch? @relation(fields: [batchId], references: [id], onDelete: SetNull)

  serialId String?
  serial   ItemSerial? @relation(fields: [serialId], references: [id], onDelete: SetNull)

  createdById String?
  createdBy   User?   @relation("InventoryMovementCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  updatedById String?
  updatedBy   User?   @relation("InventoryMovementUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([branchId, movementCode])
  @@index([branchId])
  @@index([itemId])
  @@index([batchId])
  @@index([serialId])
  @@index([type])
  @@index([source])
  @@index([movementDate])
}

model ItemSerial {
  id String @id @default(cuid())

  serialNumber String
  status       ItemSerialStatus @default(AVAILABLE)

  remarks String?

  branchId String
  branch   Branch @relation(fields: [branchId], references: [id], onDelete: Restrict)

  itemId String
  item   Item   @relation(fields: [itemId], references: [id], onDelete: Restrict)

  batchId String?
  batch   InventoryBatch? @relation(fields: [batchId], references: [id], onDelete: SetNull)

  createdById String?
  createdBy   User?   @relation("ItemSerialCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  updatedById String?
  updatedBy   User?   @relation("ItemSerialUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)

  movements InventoryMovement[]

  createdAt            DateTime              @default(now())
  updatedAt            DateTime              @updatedAt
  saleItems            SaleItem[]
  warrantyClaims       WarrantyClaim[]
  returnItems          ReturnItem[]
  stockTransferSerials StockTransferSerial[]

  @@unique([branchId, serialNumber])
  @@index([branchId])
  @@index([itemId])
  @@index([batchId])
  @@index([status])
}

model Quotation {
  id String @id @default(cuid())

  quotationCode String
  title         String?
  notes         String?
  internalNotes String?

  status QuotationStatus @default(DRAFT)

  subtotal      Decimal @default(0) @db.Decimal(12, 2)
  totalDiscount Decimal @default(0) @db.Decimal(12, 2)
  grandTotal    Decimal @default(0) @db.Decimal(12, 2)

  isPcBuild Boolean @default(false)

  validUntil  DateTime?
  sentAt      DateTime?
  approvedAt  DateTime?
  convertedAt DateTime?
  cancelledAt DateTime?

  branchId String
  branch   Branch @relation(fields: [branchId], references: [id], onDelete: Restrict)

  customerId String?
  customer   Customer? @relation(fields: [customerId], references: [id], onDelete: SetNull)

  preparedById String?
  preparedBy   User?   @relation("QuotationPreparedBy", fields: [preparedById], references: [id], onDelete: SetNull)

  serviceDoneById String?
  serviceDoneBy   User?   @relation("QuotationServiceDoneBy", fields: [serviceDoneById], references: [id], onDelete: SetNull)

  createdById String?
  createdBy   User?   @relation("QuotationCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  updatedById String?
  updatedBy   User?   @relation("QuotationUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)

  items QuotationItem[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  sales     Sale[]

  @@unique([branchId, quotationCode])
  @@index([branchId])
  @@index([customerId])
  @@index([preparedById])
  @@index([serviceDoneById])
  @@index([createdById])
  @@index([status])
  @@index([createdAt])
}

model QuotationItem {
  id String @id @default(cuid())

  lineNo           Int
  description      String
  itemCodeSnapshot String?
  itemNameSnapshot String?
  brandSnapshot    String?
  modelSnapshot    String?

  priceTier             Int
  quantity              Decimal  @db.Decimal(12, 2)
  baseUnitPriceSnapshot Decimal? @db.Decimal(12, 2)
  markupPercent         Decimal? @db.Decimal(7, 4)
  unitPrice             Decimal  @db.Decimal(12, 2)
  discountAmount        Decimal  @default(0) @db.Decimal(12, 2)
  lineTotal             Decimal  @db.Decimal(12, 2)

  isPcBuildPart Boolean @default(false)
  warrantyDuration String?
  remarks       String?

  quotationId String
  quotation   Quotation @relation(fields: [quotationId], references: [id], onDelete: Cascade)

  itemId String?
  item   Item?   @relation(fields: [itemId], references: [id], onDelete: SetNull)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([quotationId])
  @@index([itemId])
  @@index([priceTier])
}

model Sale {
  id String @id @default(cuid())

  receiptCode   String
  status        SaleStatus        @default(COMPLETED)
  paymentStatus SalePaymentStatus @default(PAID)

  saleDate DateTime @default(now())

  subtotal      Decimal @default(0) @db.Decimal(12, 2)
  totalDiscount Decimal @default(0) @db.Decimal(12, 2)
  serviceCharge Decimal @default(0) @db.Decimal(12, 2)
  grandTotal    Decimal @default(0) @db.Decimal(12, 2)
  amountPaid    Decimal @default(0) @db.Decimal(12, 2)
  changeAmount  Decimal @default(0) @db.Decimal(12, 2)

  idempotencyKey         String?
  idempotencyFingerprint String?

  remarks            String?
  cancellationReason String?
  cancelledAt        DateTime?

  branchId String
  branch   Branch @relation(fields: [branchId], references: [id], onDelete: Restrict)

  customerId String?
  customer   Customer? @relation(fields: [customerId], references: [id], onDelete: SetNull)

  quotationId String?
  quotation   Quotation? @relation(fields: [quotationId], references: [id], onDelete: SetNull)

  cashierId String?
  cashier   User?   @relation("SaleCashier", fields: [cashierId], references: [id], onDelete: SetNull)

  createdById String?
  createdBy   User?   @relation("SaleCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  updatedById String?
  updatedBy   User?   @relation("SaleUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)

  cancelledById String?
  cancelledBy   User?   @relation("SaleCancelledBy", fields: [cancelledById], references: [id], onDelete: SetNull)

  items         SaleItem[]
  payments      SalePayment[]
  creditAccount CreditAccount?

  createdAt                   DateTime                     @default(now())
  updatedAt                   DateTime                     @updatedAt
  warrantyClaims              WarrantyClaim[]
  returnRequests              ReturnRequest[]
  deliveryReceipt             DeliveryReceipt?
  incentives                  Incentive[]
  incentiveItemBasisSnapshots IncentiveItemBasisSnapshot[] @relation("IncentiveItemBasisSale")

  @@unique([branchId, receiptCode])
  @@unique([branchId, idempotencyKey])
  @@index([branchId])
  @@index([customerId])
  @@index([quotationId])
  @@index([cashierId])
  @@index([status])
  @@index([paymentStatus])
  @@index([saleDate])
}

model SaleItem {
  id String @id @default(cuid())

  lineNo      Int
  description String

  itemCodeSnapshot String?
  itemNameSnapshot String?
  brandSnapshot    String?
  modelSnapshot    String?

  priceTier                   Int?
  quantity                    Decimal  @db.Decimal(12, 2)
  baseUnitPriceSnapshot       Decimal? @db.Decimal(12, 2)
  markupPercent               Decimal? @db.Decimal(7, 4)
  unitPrice                   Decimal  @db.Decimal(12, 2)
  discountAmount              Decimal  @default(0) @db.Decimal(12, 2)
  lineTotal                   Decimal  @db.Decimal(12, 2)
  operationalUnitCostSnapshot Decimal? @db.Decimal(12, 2)
  acquisitionUnitCostSnapshot Decimal? @db.Decimal(12, 2)
  warrantyDuration            String?

  saleId String
  sale   Sale   @relation(fields: [saleId], references: [id], onDelete: Cascade)

  itemId String?
  item   Item?   @relation(fields: [itemId], references: [id], onDelete: SetNull)

  batchId String?
  batch   InventoryBatch? @relation(fields: [batchId], references: [id], onDelete: SetNull)

  serialId String?
  serial   ItemSerial? @relation(fields: [serialId], references: [id], onDelete: SetNull)

  createdAt                   DateTime                     @default(now())
  updatedAt                   DateTime                     @updatedAt
  warrantyClaims              WarrantyClaim[]
  returnItems                 ReturnItem[]
  deliveryReceiptItems        DeliveryReceiptItem[]
  incentiveItemBasisSnapshots IncentiveItemBasisSnapshot[] @relation("IncentiveItemBasisSaleItem")

  @@index([saleId])
  @@index([itemId])
  @@index([batchId])
  @@index([serialId])
}

model SalePayment {
  id String @id @default(cuid())

  paymentMethod SalePaymentMethod
  amount        Decimal           @db.Decimal(12, 2)
  referenceNo   String?
  remarks       String?
  paidAt        DateTime          @default(now())

  saleId String
  sale   Sale   @relation(fields: [saleId], references: [id], onDelete: Cascade)

  createdById String?
  createdBy   User?   @relation("SalePaymentCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([saleId])
  @@index([paymentMethod])
  @@index([paidAt])
}

model Incentive {
  id String @id @default(cuid())

  sourceKey  String          @unique
  type       IncentiveType
  status     IncentiveStatus @default(POSTED)
  sourceCode String
  sourceDate DateTime

  basisAmount            Decimal                  @db.Decimal(12, 2)
  ratePercent            Decimal                  @db.Decimal(7, 4)
  amount                 Decimal                  @db.Decimal(12, 2)
  classificationSnapshot IncentiveClassification?

  // Null means legacy/pre-V2 incentive provenance.
  engineVersion String?
  programType   IncentiveProgramType?

  postedAt       DateTime  @default(now())
  reversedAt     DateTime?
  reversalReason String?

  branchId String
  branch   Branch @relation(fields: [branchId], references: [id], onDelete: Restrict)

  staffId String
  staff   User   @relation("IncentiveStaff", fields: [staffId], references: [id], onDelete: Restrict)

  // Legacy/source-specific links remain nullable for V2 item-cycle awards.
  saleId String?
  sale   Sale?   @relation(fields: [saleId], references: [id], onDelete: Restrict)

  serviceJobId String?
  serviceJob   ServiceJob? @relation(fields: [serviceJobId], references: [id], onDelete: Restrict)

  rateVersionId String?
  rateVersion   IncentiveRateVersion? @relation(fields: [rateVersionId], references: [id], onDelete: Restrict)

  cycleId String?
  cycle   IncentiveCycle? @relation(fields: [cycleId], references: [id], onDelete: Restrict)

  programScheduleVersionId String?
  programScheduleVersion   IncentiveProgramScheduleVersion? @relation("IncentiveProgramSchedule", fields: [programScheduleVersionId], references: [id], onDelete: Restrict)

  programRuleVersionId String?
  programRuleVersion   IncentiveProgramRuleVersion? @relation("IncentiveProgramRule", fields: [programRuleVersionId], references: [id], onDelete: Restrict)

  accountConfigVersionId String?
  accountConfigVersion   IncentiveAccountConfigVersion? @relation("IncentiveAccountConfig", fields: [accountConfigVersionId], references: [id], onDelete: Restrict)

  itemCycleRevisionId String?
  itemCycleRevision   IncentiveItemCycleRevision? @relation("IncentiveItemRevisionAward", fields: [itemCycleRevisionId], references: [id], onDelete: Restrict)

  itemRecipientSnapshotId String?                         @unique
  itemRecipientSnapshot   IncentiveItemRecipientSnapshot? @relation("IncentiveItemRecipientAward", fields: [itemRecipientSnapshotId], references: [id], onDelete: Restrict)

  supersedesIncentiveId String?
  supersedesIncentive   Incentive?  @relation("IncentiveSupersession", fields: [supersedesIncentiveId], references: [id], onDelete: Restrict)
  supersededBy          Incentive[] @relation("IncentiveSupersession")

  claimLines IncentiveClaimLine[]

  postedById String?
  postedBy   User?   @relation("IncentivePostedBy", fields: [postedById], references: [id], onDelete: SetNull)

  reversedById String?
  reversedBy   User?   @relation("IncentiveReversedBy", fields: [reversedById], references: [id], onDelete: SetNull)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([branchId, sourceDate])
  @@index([staffId, sourceDate])
  @@index([type, status])
  @@index([saleId])
  @@index([serviceJobId])
  @@index([rateVersionId])
  @@index([cycleId])
  @@index([classificationSnapshot])
  @@index([engineVersion, programType])
  @@index([programScheduleVersionId])
  @@index([programRuleVersionId])
  @@index([accountConfigVersionId])
  @@index([itemCycleRevisionId])
  @@index([supersedesIncentiveId])
  @@index([postedById])
  @@index([reversedById])
}

model IncentiveAccountConfigVersion {
  id String @id @default(cuid())

  effectiveFrom    DateTime
  branchIdSnapshot String?

  classificationSnapshot IncentiveClassification

  itemEnabled     Boolean  @default(false)
  itemRatePercent Decimal? @db.Decimal(7, 4)

  ordinaryRepairEnabled     Boolean  @default(false)
  ordinaryRepairRatePercent Decimal? @db.Decimal(7, 4)

  boardRepairEnabled     Boolean  @default(false)
  boardRepairRatePercent Decimal? @db.Decimal(7, 4)

  repairFee Decimal? @db.Decimal(12, 2)

  notes String?

  accountId String
  account   User   @relation("IncentiveAccountConfigAccount", fields: [accountId], references: [id], onDelete: Restrict)

  createdById String?
  createdBy   User?   @relation("IncentiveAccountConfigCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  serviceJobs            ServiceJob[]                     @relation("ServiceJobAccountConfigVersion")
  v2Incentives           Incentive[]                      @relation("IncentiveAccountConfig")
  itemRecipientSnapshots IncentiveItemRecipientSnapshot[] @relation("IncentiveItemRecipientConfig")

  @@unique([accountId, effectiveFrom])
  @@index([accountId, effectiveFrom])
  @@index([classificationSnapshot])
  @@index([createdById])
}

model IncentiveProgramRuleVersion {
  id String @id @default(cuid())

  branchId String
  branch   Branch @relation(fields: [branchId], references: [id], onDelete: Restrict)

  programType   IncentiveProgramType
  effectiveFrom DateTime

  // Used only by ITEM_SALE.
  eligiblePriceTiers Int[] @default([])

  // Used only by repair programs.
  // For Company is derived as 100 - repairCostPercent.
  repairCostPercent Decimal? @db.Decimal(7, 4)

  notes String?

  createdById String?
  createdBy   User?   @relation("IncentiveProgramRuleCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  serviceJobs        ServiceJob[]                 @relation("ServiceJobProgramRuleVersion")
  v2Incentives       Incentive[]                  @relation("IncentiveProgramRule")
  itemCycleRevisions IncentiveItemCycleRevision[] @relation("IncentiveItemRevisionProgramRule")

  @@unique([branchId, programType, effectiveFrom])
  @@index([branchId])
  @@index([programType, effectiveFrom])
  @@index([createdById])
}

model IncentiveProgramScheduleVersion {
  id String @id @default(cuid())

  branchId String
  branch   Branch @relation(fields: [branchId], references: [id], onDelete: Restrict)

  programType  IncentiveProgramType
  scheduleType IncentiveScheduleType

  anchorDate    DateTime
  effectiveFrom DateTime

  everyNDays         Int?
  claimOpenAfterDays Int
  claimWindowDays    Int

  notes String?

  createdById String?
  createdBy   User?   @relation("IncentiveProgramScheduleCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  createdAt       DateTime         @default(now())
  updatedAt       DateTime         @updatedAt
  v2Incentives    Incentive[]      @relation("IncentiveProgramSchedule")
  incentiveCycles IncentiveCycle[] @relation("IncentiveCycleProgramSchedule")

  @@unique([branchId, programType, effectiveFrom])
  @@index([branchId])
  @@index([programType, effectiveFrom])
  @@index([scheduleType])
  @@index([createdById])
}

model IncentiveRateVersion {
  id String @id @default(cuid())

  effectiveFrom DateTime @unique
  notes         String?

  createdById String?
  createdBy   User?   @relation("IncentiveRateVersionCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  rates      IncentiveRate[]
  incentives Incentive[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([effectiveFrom])
  @@index([createdById])
}

model IncentiveRate {
  id String @id @default(cuid())

  classification IncentiveClassification
  productRate    Decimal                 @db.Decimal(7, 4)
  serviceRate    Decimal                 @db.Decimal(7, 4)

  rateVersionId String
  rateVersion   IncentiveRateVersion @relation(fields: [rateVersionId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([rateVersionId, classification])
  @@index([classification])
}

model IncentiveScheduleVersion {
  id String @id @default(cuid())

  scheduleType       IncentiveScheduleType
  anchorDate         DateTime
  effectiveFrom      DateTime              @unique
  everyNDays         Int?
  claimOpenAfterDays Int
  claimWindowDays    Int
  notes              String?

  createdById String?
  createdBy   User?   @relation("IncentiveScheduleVersionCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  cycles IncentiveCycle[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([effectiveFrom])
  @@index([createdById])
}

model IncentiveCycle {
  id String @id @default(cuid())

  periodCode     String               @unique
  startDate      DateTime
  endDate        DateTime
  cutoffDate     DateTime
  claimOpenDate  DateTime
  claimCloseDate DateTime
  status         IncentiveCycleStatus @default(EARNING)
  closedAt       DateTime?

  // Null = legacy cycle; V2 cycles explicitly use "V2".
  engineVersion String?
  programType   IncentiveProgramType?

  branchId String?
  branch   Branch? @relation("IncentiveCycleV2Branch", fields: [branchId], references: [id], onDelete: Restrict)

  // Legacy schedule path.
  scheduleVersionId String?
  scheduleVersion   IncentiveScheduleVersion? @relation(fields: [scheduleVersionId], references: [id], onDelete: Restrict)

  // V2 per-branch/per-program schedule path.
  programScheduleVersionId String?
  programScheduleVersion   IncentiveProgramScheduleVersion? @relation("IncentiveCycleProgramSchedule", fields: [programScheduleVersionId], references: [id], onDelete: Restrict)

  closedById String?
  closedBy   User?   @relation("IncentiveCycleClosedBy", fields: [closedById], references: [id], onDelete: SetNull)

  incentives         Incentive[]
  claims             IncentiveClaim[]
  itemCycleRevisions IncentiveItemCycleRevision[] @relation("IncentiveCycleItemRevisions")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Prevent duplicate V2 branch/program periods while allowing
  // legacy rows to keep nullable V2 provenance.
  @@unique([engineVersion, branchId, programType, startDate, endDate])
  @@index([scheduleVersionId])
  @@index([programScheduleVersionId])
  @@index([branchId, programType])
  @@index([status])
  @@index([claimOpenDate, claimCloseDate])
  @@index([closedById])
}

model IncentiveItemCycleRevision {
  id String @id @default(cuid())

  revisionNumber Int
  status         IncentiveStatus @default(POSTED)

  cutoffInstant              DateTime
  calculationFingerprint     String
  eligiblePriceTiersSnapshot Int[]    @default([])
  branchBasisAmountSnapshot  Decimal  @db.Decimal(12, 2)

  materializedAt DateTime
  reversedAt     DateTime?
  reversalReason String?

  cycleId String
  cycle   IncentiveCycle @relation("IncentiveCycleItemRevisions", fields: [cycleId], references: [id], onDelete: Restrict)

  programRuleVersionId String
  programRuleVersion   IncentiveProgramRuleVersion @relation("IncentiveItemRevisionProgramRule", fields: [programRuleVersionId], references: [id], onDelete: Restrict)

  createdById String?
  createdBy   User?   @relation("IncentiveItemCycleRevisionCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  reversedById String?
  reversedBy   User?   @relation("IncentiveItemCycleRevisionReversedBy", fields: [reversedById], references: [id], onDelete: SetNull)

  basisSnapshots     IncentiveItemBasisSnapshot[]
  recipientSnapshots IncentiveItemRecipientSnapshot[]
  incentives         Incentive[]                      @relation("IncentiveItemRevisionAward")

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([cycleId, revisionNumber])
  @@index([cycleId, status])
  @@index([programRuleVersionId])
  @@index([calculationFingerprint])
  @@index([createdById])
  @@index([reversedById])
}

model IncentiveItemBasisSnapshot {
  id String @id @default(cuid())

  revisionId String
  revision   IncentiveItemCycleRevision @relation(fields: [revisionId], references: [id], onDelete: Restrict)

  saleId String
  sale   Sale   @relation("IncentiveItemBasisSale", fields: [saleId], references: [id], onDelete: Restrict)

  saleItemId String
  saleItem   SaleItem @relation("IncentiveItemBasisSaleItem", fields: [saleItemId], references: [id], onDelete: Restrict)

  sourceCode              String
  sourceDate              DateTime?
  saleStatusSnapshot      String
  saleCancelledAtSnapshot DateTime?

  inclusionState  String
  inclusionReason String?
  priceTier       Int?

  grossQuantitySnapshot    Decimal @db.Decimal(12, 2)
  returnedQuantitySnapshot Decimal @db.Decimal(12, 2)
  netQuantitySnapshot      Decimal @db.Decimal(12, 2)

  baseUnitPriceSnapshot Decimal? @db.Decimal(12, 2)
  basisAmount           Decimal  @db.Decimal(12, 2)

  returnSourcesSnapshot Json

  createdAt DateTime @default(now())

  @@unique([revisionId, saleItemId])
  @@index([revisionId])
  @@index([saleId])
  @@index([saleItemId])
  @@index([inclusionState])
}

model IncentiveItemRecipientSnapshot {
  id String @id @default(cuid())

  revisionId String
  revision   IncentiveItemCycleRevision @relation(fields: [revisionId], references: [id], onDelete: Restrict)

  staffId String
  staff   User   @relation("IncentiveItemRecipientSnapshotStaff", fields: [staffId], references: [id], onDelete: Restrict)

  branchIdSnapshot       String
  roleSnapshot           String
  classificationSnapshot IncentiveClassification
  enabledSnapshot        Boolean

  ratePercentSnapshot Decimal? @db.Decimal(7, 4)
  amountSnapshot      Decimal  @db.Decimal(12, 2)

  accountConfigVersionId String?
  accountConfigVersion   IncentiveAccountConfigVersion? @relation("IncentiveItemRecipientConfig", fields: [accountConfigVersionId], references: [id], onDelete: Restrict)

  incentive Incentive? @relation("IncentiveItemRecipientAward")

  createdAt DateTime @default(now())

  @@unique([revisionId, staffId])
  @@index([revisionId])
  @@index([staffId])
  @@index([branchIdSnapshot])
  @@index([accountConfigVersionId])
}

model IncentiveClaim {
  id String @id @default(cuid())

  status                 IncentiveClaimStatus     @default(UNCLAIMED)
  classificationSnapshot IncentiveClassification?
  productBasis           Decimal                  @default(0) @db.Decimal(12, 2)
  productRate            Decimal?                 @db.Decimal(7, 4)
  productIncentive       Decimal                  @default(0) @db.Decimal(12, 2)
  serviceBasis           Decimal                  @default(0) @db.Decimal(12, 2)
  serviceRate            Decimal?                 @db.Decimal(7, 4)
  serviceIncentive       Decimal                  @default(0) @db.Decimal(12, 2)
  totalIncentive         Decimal                  @default(0) @db.Decimal(12, 2)

  claimedAt       DateTime?
  approvedAt      DateTime?
  paidAt          DateTime?
  payoutReference String?
  notes           String?

  cycleId String
  cycle   IncentiveCycle @relation(fields: [cycleId], references: [id], onDelete: Restrict)

  staffId String
  staff   User   @relation("IncentiveClaimStaff", fields: [staffId], references: [id], onDelete: Restrict)

  branchId String
  branch   Branch @relation(fields: [branchId], references: [id], onDelete: Restrict)

  claimedById String?
  claimedBy   User?   @relation("IncentiveClaimClaimedBy", fields: [claimedById], references: [id], onDelete: SetNull)

  approvedById String?
  approvedBy   User?   @relation("IncentiveClaimApprovedBy", fields: [approvedById], references: [id], onDelete: SetNull)

  paidById String?
  paidBy   User?   @relation("IncentiveClaimPaidBy", fields: [paidById], references: [id], onDelete: SetNull)

  lines IncentiveClaimLine[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([cycleId, staffId])
  @@index([branchId, status])
  @@index([staffId, status])
  @@index([claimedById])
  @@index([approvedById])
  @@index([paidById])
}

model IncentiveClaimLine {
  id String @id @default(cuid())

  type                   IncentiveType
  sourceCode             String
  sourceDate             DateTime
  classificationSnapshot IncentiveClassification?
  basisAmount            Decimal                  @db.Decimal(12, 2)
  ratePercent            Decimal                  @db.Decimal(7, 4)
  amount                 Decimal                  @db.Decimal(12, 2)

  claimId String
  claim   IncentiveClaim @relation(fields: [claimId], references: [id], onDelete: Cascade)

  incentiveId String
  incentive   Incentive @relation(fields: [incentiveId], references: [id], onDelete: Restrict)

  createdAt DateTime @default(now())

  @@unique([claimId, incentiveId])
  @@index([claimId])
  @@index([incentiveId])
  @@index([type])
}

enum CashBoxStatus {
  ACTIVE
  INACTIVE
}

enum CashTransactionType {
  CASH_IN
  CASH_OUT
  SALE_PAYMENT
  CREDIT_COLLECTION
  HANDOVER_OUT
  ADJUSTMENT_IN
  ADJUSTMENT_OUT
  SERVICE_PAYMENT
}

enum CashTransactionStatus {
  POSTED
  CANCELLED
}

enum CashTransactionSource {
  MANUAL
  SALE
  CREDIT_COLLECTION
  SYSTEM_ADJUSTMENT
  SERVICE_JOB
}

enum CashHandoverStatus {
  PENDING
  RECEIVED
  CANCELLED
}

model CashHandover {
  id String @id @default(cuid())

  handoverCode String
  status       CashHandoverStatus @default(PENDING)

  amount Decimal @db.Decimal(12, 2)

  remarks            String?
  cancellationReason String?

  receivedAt  DateTime?
  cancelledAt DateTime?

  cashBoxId String
  cashBox   CashBox @relation(fields: [cashBoxId], references: [id], onDelete: Restrict)

  branchId String
  branch   Branch @relation(fields: [branchId], references: [id], onDelete: Restrict)

  fromUserId String?
  fromUser   User?   @relation("CashHandoverFromUser", fields: [fromUserId], references: [id], onDelete: SetNull)

  toUserId String?
  toUser   User?   @relation("CashHandoverToUser", fields: [toUserId], references: [id], onDelete: SetNull)

  createdById String?
  createdBy   User?   @relation("CashHandoverCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  receivedById String?
  receivedBy   User?   @relation("CashHandoverReceivedBy", fields: [receivedById], references: [id], onDelete: SetNull)

  cancelledById String?
  cancelledBy   User?   @relation("CashHandoverCancelledBy", fields: [cancelledById], references: [id], onDelete: SetNull)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([branchId, handoverCode])
  @@index([cashBoxId])
  @@index([branchId])
  @@index([status])
  @@index([fromUserId])
  @@index([toUserId])
  @@index([createdById])
  @@index([receivedById])
  @@index([cancelledById])
}

model CashCustodianAssignment {
  id String @id @default(cuid())

  branchId String
  branch   Branch @relation(fields: [branchId], references: [id], onDelete: Restrict)

  userId String
  user   User   @relation("CashCustodianAssignedUser", fields: [userId], references: [id], onDelete: Restrict)

  assignedById String?
  assignedBy   User?   @relation("CashCustodianAssignedBy", fields: [assignedById], references: [id], onDelete: SetNull)

  assignedAt DateTime @default(now())

  endedAt DateTime?

  endedById String?
  endedBy   User?   @relation("CashCustodianEndedBy", fields: [endedById], references: [id], onDelete: SetNull)

  endReason String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([branchId])
  @@index([branchId, endedAt])
  @@index([userId])
  @@index([userId, endedAt])
  @@index([assignedById])
  @@index([endedById])
  @@index([assignedAt])
  @@index([endedAt])
}

model CashBox {
  id String @id @default(cuid())

  boxCode String
  name    String
  status  CashBoxStatus @default(ACTIVE)

  currentBalance Decimal @default(0) @db.Decimal(12, 2)
  remarks        String?

  branchId String
  branch   Branch @relation(fields: [branchId], references: [id], onDelete: Restrict)

  createdById String?
  createdBy   User?   @relation("CashBoxCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  updatedById String?
  updatedBy   User?   @relation("CashBoxUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)

  transactions CashTransaction[]

  createdAt     DateTime       @default(now())
  updatedAt     DateTime       @updatedAt
  cashHandovers CashHandover[]

  @@unique([branchId, boxCode])
  @@index([branchId])
  @@index([status])
  @@index([createdById])
  @@index([updatedById])
}

model CashTransaction {
  id String @id @default(cuid())

  transactionCode String
  type            CashTransactionType
  status          CashTransactionStatus @default(POSTED)
  source          CashTransactionSource @default(MANUAL)

  amount        Decimal @db.Decimal(12, 2)
  balanceBefore Decimal @db.Decimal(12, 2)
  balanceAfter  Decimal @db.Decimal(12, 2)

  description String
  referenceNo String?
  sourceId    String?
  sourceCode  String?

  transactionDate DateTime @default(now())

  cancelledAt        DateTime?
  cancellationReason String?

  cashBoxId String
  cashBox   CashBox @relation(fields: [cashBoxId], references: [id], onDelete: Restrict)

  branchId String
  branch   Branch @relation(fields: [branchId], references: [id], onDelete: Restrict)

  createdById String?
  createdBy   User?   @relation("CashTransactionCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  cancelledById String?
  cancelledBy   User?   @relation("CashTransactionCancelledBy", fields: [cancelledById], references: [id], onDelete: SetNull)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([branchId, transactionCode])
  @@index([cashBoxId])
  @@index([branchId])
  @@index([type])
  @@index([status])
  @@index([source])
  @@index([transactionDate])
  @@index([createdById])
  @@index([cancelledById])
}

model CreditAccount {
  id String @id @default(cuid())

  creditCode String
  status     CreditAccountStatus @default(ACTIVE)

  sourceType                ReceivableSourceType
  provider                  ReceivableProvider
  sourceTotalAmountSnapshot Decimal              @db.Decimal(12, 2)
  providerReferenceNo       String?

  idempotencyKey         String?
  idempotencyFingerprint String?

  term      InstallmentTerm?
  termBasis Decimal?         @db.Decimal(8, 4)

  cashPromoTotalAmount    Decimal? @db.Decimal(12, 2)
  regularPriceTotalAmount Decimal? @db.Decimal(12, 2)
  downpaymentAmount       Decimal  @default(0) @db.Decimal(12, 2)

  balanceAmount    Decimal  @db.Decimal(12, 2)
  monthlyDueAmount Decimal? @db.Decimal(12, 2)
  totalCollected   Decimal  @default(0) @db.Decimal(12, 2)
  remainingBalance Decimal  @db.Decimal(12, 2)

  dueDay       Int?
  firstDueDate DateTime?
  nextDueDate  DateTime?

  paidAt             DateTime?
  cancelledAt        DateTime?
  cancellationReason String?
  remarks            String?

  branchId String
  branch   Branch @relation(fields: [branchId], references: [id], onDelete: Restrict)

  customerId String?
  customer   Customer? @relation(fields: [customerId], references: [id], onDelete: Restrict)

  saleId String? @unique
  sale   Sale?   @relation(fields: [saleId], references: [id], onDelete: Restrict)

  serviceJobId String?     @unique
  serviceJob   ServiceJob? @relation(fields: [serviceJobId], references: [id], onDelete: Restrict)

  createdById String?
  createdBy   User?   @relation("CreditAccountCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  updatedById String?
  updatedBy   User?   @relation("CreditAccountUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)

  cancelledById String?
  cancelledBy   User?   @relation("CreditAccountCancelledBy", fields: [cancelledById], references: [id], onDelete: SetNull)

  collections CreditCollection[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([branchId, creditCode])
  @@unique([branchId, idempotencyKey])
  @@index([branchId])
  @@index([customerId])
  @@index([saleId])
  @@index([serviceJobId])
  @@index([status])
  @@index([sourceType])
  @@index([provider])
  @@index([term])
  @@index([nextDueDate])
  @@index([createdById])
  @@index([updatedById])
  @@index([cancelledById])
}

model CreditCollection {
  id String @id @default(cuid())

  collectionCode String
  status         CreditCollectionStatus @default(POSTED)

  amount          Decimal @db.Decimal(12, 2)
  previousBalance Decimal @db.Decimal(12, 2)
  newBalance      Decimal @db.Decimal(12, 2)

  paymentMethod SalePaymentMethod @default(CASH)
  referenceNo   String?
  remarks       String?
  paidAt        DateTime          @default(now())

  idempotencyKey         String?
  idempotencyFingerprint String?

  cancelledAt        DateTime?
  cancellationReason String?

  creditAccountId String
  creditAccount   CreditAccount @relation(fields: [creditAccountId], references: [id], onDelete: Restrict)

  branchId String
  branch   Branch @relation(fields: [branchId], references: [id], onDelete: Restrict)

  customerId String?
  customer   Customer? @relation(fields: [customerId], references: [id], onDelete: Restrict)

  collectedById String?
  collectedBy   User?   @relation("CreditCollectionCollectedBy", fields: [collectedById], references: [id], onDelete: SetNull)

  createdById String?
  createdBy   User?   @relation("CreditCollectionCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)

  cancelledById String?
  cancelledBy   User?   @relation("CreditCollectionCancelledBy", fields: [cancelledById], references: [id], onDelete: SetNull)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([branchId, collectionCode])
  @@unique([branchId, idempotencyKey])
  @@index([creditAccountId])
  @@index([branchId])
  @@index([customerId])
  @@index([status])
  @@index([paymentMethod])
  @@index([paidAt])
  @@index([collectedById])
  @@index([createdById])
  @@index([cancelledById])
}
